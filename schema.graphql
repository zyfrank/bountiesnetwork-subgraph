type Bounty @entity{
  id: ID!
  issuer: Bytes!
  deadline: BigInt!
  data: String!
  fulfillmentAmount: BigInt!
  arbiter: Bytes!
  paysTokens: Boolean!
  bountyStage: BountyStages!
  balance: BigInt!
}

type Fulfillment @entity{
      id: ID!
      accepted: Boolean!
      fulfiller: Bytes!
      data: String!
}

enum BountyStages {
      Draft,
      Active,
      Dead
}