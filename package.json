{
  "name": "bountiesnetwork-subgraph",
  "version": "0.1.0",
  "scripts": {
    "create": "graph create zyfrank/Bountiesnetwork --node https://api.thegraph.com/deploy/",
    "create-local": "graph create zyfrank/Bountiesnetwork --node http://127.0.0.1:8020",
    "codegen": "graph codegen --debug --output-dir src/types/",
    "build": "graph build --debug",
    "deploy": "graph deploy zyfrank/Bountiesnetwork --debug --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-local": "graph deploy zyfrank/Bountiesnetwork --debug --ipfs http://localhost:5001 --node http://127.0.0.1:8020"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.11.2",
    "@graphprotocol/graph-ts": "^0.11.0"
  },
  "dependencies": {

  }
}